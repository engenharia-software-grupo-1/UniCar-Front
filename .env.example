# URL base da API de backend (US6). Padrão: http://localhost:8080
VITE_API_URL=http://localhost:8080

# Mocka a API no navegador (MSW) durante `npm run dev`.
# Default false: o dev bate na API real. Coloque true para rodar a UI sem backend.
VITE_ENABLE_MOCKS=false
